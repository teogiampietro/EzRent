@attribute [Route($"{Constants.ROUTE_PROPERTY_ADD}")]
@inject HttpClient Http;
@inject NavigationManager NavManager

<MudText Class="my-3" Typo="Typo.h4">Agregar propiedad</MudText>
<FormProperty ButtonText="Agregar" Property="@Property" OnValidSubmit="@CreateProperty"/> 
